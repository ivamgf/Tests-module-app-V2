import React from "react";

import './header.css';
import Navbar from '../navbar/Navbar';

const Header = () => (
    <header id="main-header">
        <Navbar /> 
    </header>    
);

export default Header;
